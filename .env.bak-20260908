# CloudCLI — configuracion de esta maquina
# OJO: las variables reales del entorno (systemd Environment=) MANDAN sobre
# este archivo (server/load-env.ts:31 solo setea si la key no existe todavia).
# Este .env es para cuando se corre a mano; la unidad de systemd repite el bind.

# --- RED: solo Tailscale. NUNCA 0.0.0.0 ni 45.141.22.170 (IP publica) ---
HOST=100.77.186.53
SERVER_PORT=3001
VITE_PORT=5173

# --- Claude Code ---
CLAUDE_CLI_PATH=/home/leantejado/.local/bin/claude
CONTEXT_WINDOW=160000
VITE_CONTEXT_WINDOW=160000
